# Build output and dependencies
.next/
out/
node_modules/

# Playwright output
playwright-report/
test-results/

# Generated by Next.js, not ours to format
next-env.d.ts

# Lockfile: npm owns the formatting
package-lock.json

# Recorded API responses. These are captured from the running backend as-is;
# reformatting them would obscure the next re-capture's real diff. The README
# beside them is ours, so it stays formatted.
demo-data/*.json
